<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Perfil do Usuário</title>
  <style>
      body {
          display: flex;
          flex-direction: column;
          align-items: center;
          margin: 0;
          background-color: #f9f9f9; /* Leve cinza */
          font-family: Arial, sans-serif;
      }
      h1 {
          color: #4B0082; /* Roxo escuro */
          padding-top: 15px;
          padding-bottom: 30px;
          margin: 0;
      }
      p {
          margin: 20px 0;
          color: #333; /* Texto escuro */
      }
      button {
          background-color: #D5006D; /* Rosa vibrante */
          color: white;
          border: none;
          padding: 10px 20px;
          border-radius: 5px;
          cursor: pointer;
          transition: background-color 0.3s;
      }
      button:hover {
          background-color: #C51162; /* Rosa mais escuro */
      }
      table {
          margin-top: 20px;
          border-collapse: collapse;
          width: 90%;
          text-align: left;
          background-color: white;
          box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
          border-radius: 8px;
          overflow: hidden;
      }
      th, td {
          border: 1px solid #e0e0e0; /* Borda leve */
          padding: 10px;
      }
      th {
          background-color: #4B0082; /* Roxo escuro */
          color: white;
      }
      td {
          background-color: #fafafa; /* Cinza muito claro */
      }
      img {
          width: 100px;
          height: 100px;
          border-radius: 5px;
      }
      a {
          color: #D5006D; /* Rosa vibrante */
          text-decoration: none;
          font-weight: bold;
          margin-top: 20px;
      }
      a:hover {
          text-decoration: underline;
      }
  </style>
</head>
<body>
  <h1>Bem-vindo, <%= userName %>!</h1>
  <p><font color="black" face="arial"> Seja Bem-vindo á sua página de perfil. </font></p>
    
  <!-- Link para cadastrar um novo livro -->
  <button><a href="/editar-perfil" style="color: white;">Editar Perfil</a></button>
  <br/> <br/>
   
  <h2>Seus Livros</h2>
  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Título</th>
        <th>Autor</th>
        <th>Gênero</th>
        <th>Descrição</th>
        <th>Data de Lançamento</th>
        <th>Imagem</th>
      </tr>
    </thead>
    <tbody>
      <% livros.forEach(livro => { %>
        <tr>
          <td><%= livro.id_livro %></td>
          <td><%= livro.titulo %></td>
          <td><%= livro.autor %></td>
          <td><%= livro.genero %></td>
          <td><%= livro.descricao %></td>
          <td><%= livro.data_lanc %></td>
          <td>
            <% if (livro.imagem) { %>
              <img src="/uploads/<%= livro.imagem %>" alt="Imagem do Livro">
            <% } else { %>
              Sem imagem
            <% } %>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>

  <a href="/logout">Logout</a>
</body>
</html>
